# إضافة قسم "College Student" (الطالب) — شرح كامل

توثيق للتعديل اللي اتعمل على منصة **Physio Motion** لإضافة نوع حساب تالت مستقل بالكامل: **College Student**، منفصل عن حساب الدكتور وحساب المريض.

---

## 1. الفكرة العامة

قبل كده كان فيه نوعين حساب بس:
- **Patient** (مريض)
- **Doctor** (دكتور)

دلوقتي ضفنا نوع تالت:
- **College Student** (طالب كلية علاج طبيعي)

الطالب ليه **داشبورد خاصة بيه لوحده**، مش هو دكتور ومش هو مريض — منظومة تالتة معزولة، بتشارك بس الصفحات اللي فعلاً محتاجها الطالب (Guidelines + Muscle Test) واللي أصلاً موجودة عند الدكتور، وباقي كل حاجة تانية (شات، مواعيد، خطط علاج... إلخ) الطالب مش شايفها خالص ومش عنده وصول ليها.

---

## 2. رحلة التسجيل والدخول

### صفحة `login.html`
- في تبويب "Create Account" بقى فيه 3 اختيارات بدل 2:
  - 🧑‍🦽 Patient
  - 👨‍⚕️ Doctor
  - 🎓 **College Student** (جديد)
- الاختيار بيتخزن في نفس الـ `<select id="signupRole">` القديم بقيمة `student`، فمفيش أي تعديل في منطق حفظ اليوزر — هو نفس الكود بتاع `doSignup()` في `js/login.js`، بس دلوقتي بيقبل قيمة `role = 'student'` زي ما بيقبل `doctor` و `patient`.

### بعد تسجيل الدخول
- لو `role === 'doctor'` → يروح `choose-year.html` (داشبورد الدكتور).
- لو `role === 'student'` → يروح `student-dashboard.html` (**الصفحة الجديدة**).
- لو غير كده (`patient`) → يروح `choose-role.html` (داشبورد المريض).

التوجيه ده اتظبط في 3 أماكن:
1. `js/login.js` → بعد `doLogin()` بينجح.
2. `login.html` → لو اليوزر أصلاً عامل لوجين (session موجودة) وفتح صفحة اللوجين تاني.
3. `js/utils.js` → دالة جديدة اسمها `dashboardUrlForRole(role)` بترجع اللينك الصح حسب الدور، وبتتستخدم في أي مكان تاني محتاج يعرف "داشبورد مين" (زي `goHome()` و `requireAuth()`).

---

## 3. داشبورد الطالب — `student-dashboard.html` (صفحة جديدة بالكامل)

صفحة مستقلة بتصميمها الخاص (مبنية على نفس ستايل داشبورد المريض/الدكتور عشان تفضل متسقة مع باقي المنصة)، وفيها:

### الهيدر
- زرار **Profile** 👤
- زرار **Logout** 🚪
- **من غير زرار شات خالص** (زي ما اتطلب — الطالب مفيش عنده شات).

### الكروت (Modules)

| الكارت | الحالة | بيوديك فين |
|---|---|---|
| 📚 Clinical Guidelines | شغال فعليًا | `guidelines.html` — **نفس الصفحة بالظبط** اللي بيستخدمها الدكتور، بكل الداتا والبحث الذكي |
| 💪 Muscle Testing | شغال فعليًا | `muscle-test.html` — **نفس الصفحة بالظبط** اللي بيستخدمها الدكتور (الجريدات، العضلات، الفيديوهات) |
| 🗣️ Physio Communication Center | 🔒 مقفول — Coming Soon | مفيش لينك، لما يدوس عليه يطلعله toast صغير "Coming soon" |
| 🎥 Physio Lessons | 🔒 مقفول — Coming Soon | نفس الحالة |
| 🦴 Anatomy | 🔒 مقفول — Coming Soon | نفس الحالة |

الكروت المقفولة اتحطلها Badge صغير "🔒 Coming Soon" في ركن الكارت، وبتفتح تنبيه بسيط تحت الشاشة (toast) بدل ما تودي لصفحة فاضية أو تعطي error.

---

## 4. إزاي الطالب بيستخدم صفحات الدكتور (Guidelines + Muscle Test) من غير ما يبوظ حسابه

الصفحتين دول أصلاً كانوا مقفولين على `requireAuth('doctor')` بس، يعني أي حد مش دكتور يترفض دخوله. اتعمل فيهم تعديلين:

1. **`js/utils.js`** — دالة `requireAuth()` بقت تقبل **أكتر من دور مسموح** مش دور واحد بس:
   ```js
   requireAuth(['doctor', 'student'])
   ```
   لو اليوزر مش دكتور ولا طالب، بترجعه تلقائيًا لداشبوردته الصح (`dashboardUrlForRole`).

2. **`guidelines.html`** و **`muscle-test.html`**:
   - اتغير الشرط من `requireAuth('doctor')` لـ `requireAuth(['doctor','student'])`.
   - زرار "🏠 Dashboard" بقى بيستخدم `goHome()` بدل ما يكون مربوط بـ `choose-year.html` بالظبط — عشان يرجّع الدكتور لداشبورده والطالب لداشبورده هو تلقائيًا.
   - في `muscle-test.html` تحديدًا، زرار "← Assessments" (اللي بيودي لصفحة Assessments المخصوصة للدكتور بس) بقى للطالب بيتحول تلقائيًا لـ "← Dashboard" وبيرجعه لداشبورد الطالب بدل ما يحاول يفتح صفحة مش مسموح له بيها.

> ملحوظة مهمة: الصفحتين دول (Guidelines و Muscle Test) صفحات **مرجعية فقط** — مفيش فيهم أي حفظ بيانات مرتبطة بمريض معين أو أي كتابة على قاعدة البيانات، فمفيش أي خطر إن الطالب يستخدمهم أو يأثر على بيانات دكتور أو مريض.

---

## 5. صفحة البروفايل (`profile.html` / `js/profile.js`)

كانت أصلاً فيها دعم جزئي لدور "Student" من قبل، فاتظبطت تشتغل صح بالكامل:

### الحقول المشتركة (زي أي حساب):
- الاسم الكامل
- رقم التليفون
- نبذة عني (Bio)

### حقول خاصة بالطالب بس (مختلفة تمامًا عن حقول الدكتور):
- 🎓 **اسم الجامعة / الكلية**
- 🎓 **السنة الدراسية** (سنة 1 → 5، أو "امتياز / Internship")

### إتشال بالكامل من حساب الطالب:
- ❌ خانة **Specialty** (التخصص) — كانت خاصة بالدكتور بس، مش موجودة للطالب أصلًا.
- ❌ **العنوان / مكان العيادة أو الشغل** (Clinic/Address) — بردو خاصة بالدكتور فقط.

يعني حساب الطالب شكله كده:
```
الاسم | التليفون | نبذة  →  ثم  →  اسم الجامعة | السنة الدراسية (5 أو امتياز)
```
من غير أي تخصص أو مكان عمل، بالظبط زي ما اتطلب.

كمان في عرض الدور بالأعلى (`Role: —`) بقى بيظهر "College Student" بدل الكلمة الخام `student`.

---

## 6. ملخص الملفات اللي اتعدلت / اتضافت

| الملف | نوع التعديل |
|---|---|
| `login.html` | إضافة اختيار "College Student" في شاشة التسجيل |
| `js/login.js` | تعديل توجيه الطالب بعد تسجيل الدخول |
| `js/utils.js` | `requireAuth()` بقت تقبل أكتر من دور + دالة جديدة `dashboardUrlForRole()` + تحديث `goHome()` |
| `guidelines.html` | السماح لدور الطالب بالدخول + تعديل زرار الداشبورد |
| `muscle-test.html` | السماح لدور الطالب بالدخول + تعديل زراير الرجوع |
| `js/profile.js` | عرض اسم الدور "College Student" بدل `student` |
| `service-worker.js` | إضافة الصفحة الجديدة لقائمة الكاش (Offline support) |
| `index.html` | إضافة شرح دور الطالب في الصفحة الرئيسية (تفاصيل في القسم 8) |
| **`student-dashboard.html`** | 🆕 **صفحة جديدة بالكامل** — داشبورد الطالب المستقلة |

---

## 8. صفحة الهبوط الرئيسية `index.html` — إضافة شرح دور الطالب

عشان أي زائر جديد يعرف إن فيه دور تالت للطلبة قبل ما يعمل حساب، اتضاف شرح للدور في 3 أماكن بصفحة الهبوط:

### أ) قسم "For..." في الهيرو (أول الصفحة)
كان فيه كارتين بس: **For Patients** و **For Physiotherapists**. اتضاف كارت تالت بينهم اسمه **"For Physiotherapy Students"** 🎓 بلون بنفسجي مميز، بيشرح باختصار:
- نفس الـ Clinical Guidelines و MMT Reference اللي عند الدكتور.
- إشارة إن باقي الأقسام (Lessons, Anatomy, Communication Center) جاية قريب.

### ب) قسم "Two/Three Roles, One Platform" (تحت الصفحة)
- العنوان اتغير من **"Two Roles, One Platform"** لـ **"Three Roles, One Platform"**.
- العنوان الفرعي اتغير من **"Built for Doctors & Patients"** لـ **"Built for Doctors, Patients & Students"**.
- اتضاف كارت تالت كامل بعنوان **"College Student"** جنب كارت الـ Physiotherapist وكارت الـ Patient، وفيه قائمة بكل حاجة الطالب يقدر يعملها:
  - البحث في Clinical Guidelines بالـ AI.
  - مرجع الـ MMT الكامل.
  - بروفايل خاص بيه (جامعة + سنة دراسية، من غير إعداد عيادة).
  - الأقسام الجديدة (Communication Center / Lessons / Anatomy) موضحة إنها "coming soon".
- الشبكة (`grid`) بتاعة الكروت دي اتظبطت تستوعب 3 كروت جنب بعض على الشاشات الكبيرة، وتترتب تلقائي لعمود واحد أو اتنين على الموبايل.

### ج) خطوة "Create Account" في قسم "The Process"
النص كان بيقول "Doctor or Patient — register in seconds"، اتغير لـ **"Doctor, Patient, or College Student — register in seconds"** عشان يعكس الاختيار التالت من أول خطوة في رحلة المستخدم.

مفيش أي تعديل في المنطق أو الفانكشنز في `index.html` — كل التعديلات دي محتوى نصي/تصميمي بس (Marketing content) عشان تعرّف بدور الطالب الجديد لأي حد بيزور الموقع.


---

## 7. اللي لسه "Coming Soon" ومحتاج شغل لاحقًا

الأقسام التالتة دول دلوقتي مجرد كروت مقفولة بواجهة جاهزة، ولسه محتاجين نبني فيهم المحتوى الفعلي والمنطق البرمجي:

1. **Physio Communication Center** 🗣️ — قسم تواصل بين الطلبة و/أو المشرفين.
2. **Physio Lessons** 🎥 — دروس ومحاضرات منظمة.
3. **Anatomy** 🦴 — مرجع تشريحي تفاعلي.

كل ما نجهز واحد منهم، هنفك القفل بتاعه في `student-dashboard.html` ونحوّل onclick بتاعه من `comingSoon('...')` لصفحة حقيقية.
